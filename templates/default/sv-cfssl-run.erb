#!/bin/sh
exec 2>&1

[ -d env ] && setenv="-e $(pwd)/env"

exec chpst $setenv <%=node['go']['gobin']%>/cfssl serve -loglevel 0 \
-port <%= @options[:port] %> \
-ca-bundle <%= @options[:ca] %> \
-ca-key <%= @options[:key] %> \
-ca <%= @options[:ca] %> \
-config <%= @options[:config] %>
